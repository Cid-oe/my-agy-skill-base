# Project Spec

Category: planning | Priority: high | Cost: low | Latency: medium

**Purpose:** Produce a short, concrete spec before significant build work starts, so scope and 'done' are explicit rather than discovered along the way.

**Use when:** Use before starting substantial new work — a new feature, service, or project — to write a short spec (goal, scope, non-goals, constraints, done-criteria) so the build has a clear target.

**Consumes:** ActionableRequest  
**Produces:** Spec
