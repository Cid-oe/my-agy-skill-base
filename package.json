{
  "name": "agy-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AGY AI Operating System Monorepo",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "pretest": "npm run build",
    "test": "node scripts/run-tests.mjs",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
